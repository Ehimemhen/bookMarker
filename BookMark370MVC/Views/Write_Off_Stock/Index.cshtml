@model IEnumerable<BookMark370MVC.Models.Write_Off_Stock>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Inventory.Inventory_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Inventory.Inventory_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Write_Off_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Write_Off_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Write_Off_ID })
        </td>
    </tr>
}

</table>

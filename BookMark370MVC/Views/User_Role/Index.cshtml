@model IEnumerable<BookMark370MVC.Models.User_Role>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserRole_Description)
        </th>
     
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserRole_Description)
        </td>
    
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.UserRole_ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.UserRole_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.UserRole_ID })
        </td>
    </tr>
}

</table>
<p>
    @Html.ActionLink("View All Employees?", "Index", "Employees")
</p>